import { NavBarDropdownDirective } from './nav-bar-dropdown.directive';

describe('NavBarDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new NavBarDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
