import { NewPopUpDirective } from './new-pop-up.directive';

describe('NewPopUpDirective', () => {
  it('should create an instance', () => {
    const directive = new NewPopUpDirective();
    expect(directive).toBeTruthy();
  });
});
